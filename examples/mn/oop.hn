төрөл Цэг = { x, y }

функц шинэ_цэг(x_val, y_val) {
    буцаах Цэг(x: x_val, y: y_val)
}

функц урт(p) {
    буцаах p.x * p.x + p.y * p.y
}

функц main() {
    хувьсагч p = шинэ_цэг(3, 4)
    хэвлэх(урт(p))
}
